# Check if Object is a Fitted ML Model

Check if Object is a Fitted ML Model

## Usage

``` r
is_ml_fitted(x)
```

## Arguments

- x:

  Object to check.

## Value

Logical indicating if `x` is an `ml_fitted` object.
