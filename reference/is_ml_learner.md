# Check if Object is an ML Learner Specification

Check if Object is an ML Learner Specification

## Usage

``` r
is_ml_learner(x)
```

## Arguments

- x:

  Object to check.

## Value

Logical indicating if `x` is an `ml_learner` object.
