% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{as.data.frame.tr_roc}
\alias{as.data.frame.tr_roc}
\alias{as.data.frame.cf_roc}
\title{Convert ROC Curve to Data Frame}
\usage{
\method{as.data.frame}{tr_roc}(x, ...)

\method{as.data.frame}{cf_roc}(x, ...)
}
\arguments{
\item{x}{An object of class \code{tr_roc} or \code{cf_roc}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A data frame with columns:
\item{threshold}{Classification threshold}
\item{fpr}{False positive rate}
\item{sensitivity}{Sensitivity (TPR)}
\item{specificity}{Specificity}
\item{type}{Either "adjusted" or "naive"}
}
\description{
Converts an ROC curve object to a data frame suitable for use with ggplot2.
}
\examples{
set.seed(123)
n <- 500
x <- rnorm(n)
a <- rbinom(n, 1, 0.5)
y <- rbinom(n, 1, plogis(-1 + x))
pred <- plogis(-1 + 0.8 * x)

roc <- cf_roc(
  predictions = pred,
  outcomes = y,
  treatment = a,
  covariates = data.frame(x = x),
  n_thresholds = 21
)
df <- as.data.frame(roc)
head(df)
}
